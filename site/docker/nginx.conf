server {
  listen 80;
  listen [::]:80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;
  error_page 404 /404.html;

  location = / {
    return 302 /en/;
  }

  location / {
    try_files $uri $uri/ $uri/index.html =404;
  }
}
