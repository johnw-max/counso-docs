{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=9.6.5"
  },
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "generate:release": "node scripts/generate-release.mjs",
    "check:release": "node scripts/check-release.mjs",
    "prebuild": "python3 ../scripts/refresh_metadata.py && python3 ../mapping/build_routes.py && node scripts/generate-release.mjs",
    "build": "astro build",
    "postbuild": "node scripts/postbuild-compat.mjs",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.42.0",
    "astro": "^7.2.10",
    "sharp": "^0.35.3"
  }
}
