# 表格分析为什么不完整或不准确？

先确认智能体能访问分析所需的所有表格或文件。CSV 或电子表格的首行应包含列名，说明性文字不要混在数据行里。

若多个表有关联，应明确说明关联键和关系，例如“按 supplier_id 将发票行匹配到供应商表”。让智能体列出使用的行和假设，再抽查少量记录后核对汇总数。

对于 Notion 数据库，表格视图不会自动包含其关联数据库。请确认 Relation 或 Rollup 属性涉及的每个数据库也都在智能体可访问的数据范围内。
