# Choose the right model for the task

## Compare models fairly

Test the same representative request with each model available to your workspace. Compare factual accuracy, source use, output format, latency, and resource use; select the least costly option that meets the review standard. Treat the current model picker as authoritative because availability changes.

For automatic selection, available tiers, and switching models, see [Model selection](../model-selection.md).
