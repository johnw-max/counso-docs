# Can Agents access the internet?

Only if web search or browsing is enabled for that Agent and available in the workspace. Without it, the Agent works from its instructions, conversation, and configured sources.

For a web-based answer, ask it to identify the pages it used and verify important facts on the original sites.
