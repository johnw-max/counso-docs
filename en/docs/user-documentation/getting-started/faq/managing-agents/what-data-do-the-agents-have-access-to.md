# What data can an Agent access?

An Agent can use the sources configured for it, subject to the permissions of the current user and connected account. Access can differ by Agent, Space, Pod, and source.

Check the Agent’s data settings and the source’s own permissions. Shared conversations do not bypass source controls, and a file’s presence in a folder is not a substitute for an appropriate access boundary.
