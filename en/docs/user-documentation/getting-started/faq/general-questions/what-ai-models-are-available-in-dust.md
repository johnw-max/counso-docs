# Which AI models are available in Counso?

The model picker shows the models available to you. Administrators may limit model choices, and the list can change as workspace settings and provider availability change.

Choose a model that fits the task and workspace guidance. If a model is missing, ask your administrator to check model access.
